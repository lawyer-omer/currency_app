<%= form_with(model: rate) do |form| %>
  <% if rate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
        <% rate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.select :from_symbol, (["USD", "EUR", "TRY", "CAD", "GBP", "JPY", "QAR", "AED"])%>
  </div>

  <div>
    <%= form.select :to_symbol, (["USD", "EUR", "TRY", "CAD", "GBP", "JPY", "QAR", "AED"])%>
  </div>

  <div>
    <%= form.text_field :rate_symbol, type: "hidden" %>
  </div>

  <div>
    <%= form.number_field :user_id, value: current_user.id, type: "hidden" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
